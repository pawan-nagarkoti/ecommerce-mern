{
  "name": "mern-dev-setup",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c green,blue \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "cookieparser": "^0.1.0"
  }
}
